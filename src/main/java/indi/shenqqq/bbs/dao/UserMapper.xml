<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="indi.shenqqq.bbs.dao.UserMapper">

    <select id="selectAll" resultType="map">
        select u.id,u.username,u.avatar,u.email,u.in_time
        from  user u
    </select>

    <select id="search" resultType="map">
        select u.id,u.username,u.avatar,u.email,u.in_time
        from user u
        <where>
            u.username like concat(concat('%',#{keyword}),'%') or u.id = #{keyword} or u.email like concat(concat('%',#{keyword}),'%')
        </where>
    </select>

</mapper>
