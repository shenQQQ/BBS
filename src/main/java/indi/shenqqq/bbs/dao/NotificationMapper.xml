<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="indi.shenqqq.bbs.dao.NotificationMapper">

    <select id="selectByUserId" resultType="map">
        select n.*, uu.username, uu.avatar, a.title, a.id as articleId
        from notification n
        left join user u on u.id = n.target_user_id
        left join user uu on uu.id = n.user_id
        left join article a on a.id = n.article_id
        where n.target_user_id = #{userId} and n.`isread` = #{read}
        <if test="limit &gt; 0">
            limit #{limit}
        </if>
    </select>

    <select id="selectAll" resultType="map">
        select n.*, uu.username, uu.avatar, a.title, a.id as articleId
        from notification n
        left join user u on u.id = n.target_user_id
        left join user uu on uu.id = n.user_id
        left join article a on a.id = n.article_id
        where n.target_user_id = #{userId}
        order by in_time desc
    </select>

</mapper>
